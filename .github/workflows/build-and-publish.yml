name: Build images

on:
  push:
    branches: [ "main" ]

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    steps:
    - uses: actions/checkout@v4
    
    - name: Login to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
        
    - name: setup builder
      run: docker buildx create --name multi-arch --use --bootstrap --platform linux/amd64,linux/arm64
      
    - name: build frontend
      run: docker buildx build --push --tag ${{env.IMAGE_NAME}}-web:latest frontend

    - name: build frontend
      run: docker buildx build --push --tag ${{env.IMAGE_NAME}}-api:latest backend
      
